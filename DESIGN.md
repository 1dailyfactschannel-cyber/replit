# TeamSync — Руководство по дизайну

Полное руководство по дизайну системы управления проектами TeamSync. Используйте этот документ как справочник для создания похожих интерфейсов.

---

## 📋 Содержание

1. [Философия дизайна](#философия-дизайна)
2. [Цветовая система](#цветовая-система)
3. [Типография](#типография)
4. [Компоненты и их использование](#компоненты-и-их-использование)
5. [Макеты и структура](#макеты-и-структура)
6. [Интерактивные элементы](#интерактивные-элементы)
7. [Практические советы](#практические-советы)

---

## 🎨 Философия дизайна

### Основные принципы

**TeamSync** следует философии "утончённого минимализма":

- **Простота с характером**: Не плоский, не перегруженный. Каждый элемент имеет цель.
- **Доступность**: Контрасты соответствуют WCAG AA. Текст читаемый на любом фоне.
- **Глубина**: Используются тонкие тени, градиенты и прозрачность для добавления объёма.
- **Микро-взаимодействия**: Плавные переходы, hover-эффекты, анимации для живого ощущения.

### Эмоциональное направление

Дизайн вызывает ощущения:
- **Профессионализма**: серьёзный, надёжный
- **Современности**: свежий, актуальный
- **Порядка**: организованный, структурированный
- **Сотрудничества**: открытый, доступный

---

## 🎭 Цветовая система

### Основная палитра (Light Mode)

```css
/* Основной фон - очень светлый, прохладный серый */
--background: 210 40% 98%;      /* #F0F7FF */

/* Текст - глубокий сланец */
--foreground: 222 47% 11%;      /* #0F172A */

/* Основной цвет - индиго */
--primary: 230 70% 55%;         /* #4F46E5 */

/* Вторичный - мягкий, светлый */
--secondary: 210 40% 96.1%;     /* #F0F5FB */

/* Неактивный текст - средний серый */
--muted-foreground: 215 16% 47%;  /* #647099 */

/* Граница - лёгкий серый */
--border: 214 32% 91%;          /* #DDE5F0 */
```

### Тёмный режим (Dark Mode)

```css
/* Фон - глубокий сланец */
--background: 222 47% 11%;      /* #0F172A */

/* Текст - светлый */
--foreground: 210 40% 98%;      /* #F0F7FF */

/* Основной цвет - яркое индиго */
--primary: 230 70% 60%;         /* #6366F1 */

/* Граница - тёмный сланец */
--border: 217 33% 20%;          /* #1E293B */
```

### Как использовать цвета

1. **Фон страницы**: Всегда используйте `--background`
2. **Основной текст**: Используйте `--foreground`
3. **Действия**: Кнопки, ссылки — используйте `--primary`
4. **Вспомогательный текст**: Подписи, хинты — используйте `--muted-foreground`
5. **Разделители**: Линии, границы — используйте `--border`
6. **Статусы**:
   - Успех: `bg-emerald-500` (зелёный)
   - Ошибка: `bg-rose-500` (красный)
   - Предупреждение: `bg-amber-500` (оранжевый)
   - Информация: Используйте `--primary`

### Контрастность

Все текст-фон комбинации протестированы на WCAG AA:
- Основной текст на фоне: 12.5:1 контраст
- Мягкий текст на фоне: 5.2:1 контраст
- Интерактивные элементы: минимум 3:1

---

## ✍️ Типография

### Шрифты

```css
/* Заголовки и логотип - смелый, современный */
font-family: 'Plus Jakarta Sans', sans-serif;

/* Основной текст - чистый, читаемый */
font-family: 'Inter', sans-serif;
```

Оба шрифта импортируются из Google Fonts в `client/index.html`.

### Иерархия размеров

```
H1 (Заголовки страниц): 30px, font-weight: 700, letter-spacing: -0.5px
H2 (Заголовки секций):  24px, font-weight: 600
H3 (Подзаголовки):      20px, font-weight: 600
H4 (Заголовки карточек):18px, font-weight: 600
Body (Основной текст):  14px, font-weight: 400
Small (Вспомогательный):12px, font-weight: 400
Caption (Подписи):      11px, font-weight: 500
```

### Как применять

```jsx
// Заголовок страницы
<h1 className="text-3xl font-bold tracking-tight">Обзор</h1>

// Подзаголовок
<p className="text-muted-foreground mt-1">Описание...</p>

// Основной текст в карточках
<p className="text-sm">Содержимое...</p>

// Маленькие метки
<span className="text-xs text-muted-foreground">Вспомогательный текст</span>
```

---

## 🧩 Компоненты и их использование

### Card (Карточка)

Базовый контейнер для содержимого.

```jsx
<Card className="border-border/50 shadow-sm hover:shadow-md transition-shadow">
  <CardHeader>
    <CardTitle>Заголовок</CardTitle>
    <CardDescription>Подзаголовок</CardDescription>
  </CardHeader>
  <CardContent>
    {/* Содержимое */}
  </CardContent>
</Card>
```

**Когда использовать**:
- Отдельные блоки информации
- Статистика
- Панели с данными
- Модальные окна

**Цвета**: `bg-card` с `border-border/50` и `shadow-sm`

### Button (Кнопка)

```jsx
// Основная кнопка - для главных действий
<Button className="shadow-lg shadow-primary/20">Создать</Button>

// Вторичная - для дополнительных действий
<Button variant="outline">Отмена</Button>

// Ghost - для неяркого интерфейса
<Button variant="ghost" size="icon"><Icon /></Button>
```

**Варианты**:
- По умолчанию: `bg-primary text-primary-foreground`
- `outline`: Граница без фона
- `ghost`: Прозрачная, проявляется при hover

### Badge (Метка)

Для статусов, тегов, категорий.

```jsx
// Для статуса - цветная
<Badge className="bg-purple-100 text-purple-700">Дизайн</Badge>

// Для счётчика
<span className="text-xs text-muted-foreground bg-secondary px-2 py-0.5 rounded-full">
  5
</span>
```

### Avatar (Аватар)

Для пользователей и команд.

```jsx
<Avatar>
  <AvatarImage src="url" />
  <AvatarFallback>ЮД</AvatarFallback>
</Avatar>

// С индикатором статуса
<div className="relative">
  <Avatar />
  <span className="absolute bottom-0 right-0 w-3 h-3 bg-emerald-500 rounded-full border-2 border-background" />
</div>
```

### Input (Поле ввода)

```jsx
// В header с поиском
<Input
  placeholder="Поиск..."
  className="pl-9 bg-secondary/50 border-transparent hover:bg-secondary focus:bg-background transition-colors rounded-xl"
/>
```

### Sidebar (Боковая панель)

```jsx
<div className="bg-sidebar text-sidebar-foreground border-r border-sidebar-border">
  {/* Навигация */}
</div>
```

**Особенности**:
- Цвет фона отличается от основного для контраста
- Активный пункт: `bg-sidebar-primary text-sidebar-primary-foreground`
- Неактивный: `text-muted-foreground hover:bg-sidebar-accent`

---

## 📐 Макеты и структура

### Главный Layout

```
┌────────────────────────────────────────────┐
│  HEADER (h-16) с поиском и уведомлениями  │
├──────────────┬──────────────────────────────┤
│              │                              │
│  SIDEBAR     │   MAIN CONTENT               │
│  (w-64)      │   (flex-1, overflow-y-auto) │
│              │                              │
│              │                              │
└──────────────┴──────────────────────────────┘
```

**Код структуры**:

```jsx
<div className="min-h-screen bg-background flex">
  {/* Desktop Sidebar */}
  <div className="hidden md:block w-64 h-screen sticky top-0">
    <SidebarContent />
  </div>

  {/* Main Content */}
  <div className="flex-1 flex flex-col min-h-screen">
    <header className="h-16 border-b sticky top-0">
      {/* Header content */}
    </header>
    <main className="flex-1 p-6 md:p-8 overflow-y-auto">
      {children}
    </main>
  </div>
</div>
```

### Сетка для карточек

```jsx
// 4 колонны на большом экране, 2 на среднем, 1 на маленьком
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
  {items.map(item => <Card key={item.id}>{item}</Card>)}
</div>
```

### Расстояния (Spacing)

Используется система `0.25rem` (4px) базовой единицы:

```
p-1   = 4px
p-2   = 8px
p-3   = 12px
p-4   = 16px
p-6   = 24px
p-8   = 32px
gap-2 = 8px
gap-4 = 16px
gap-6 = 24px
```

**Правило**: Используйте **чётные числа** (4, 8, 12, 16, 24, 32) для консистентности.

### Радиус углов

```
rounded-lg = 0.75rem (12px)
rounded-xl = 1rem (16px)
```

Карточки обычно используют `rounded-xl` для современного вида.

---

## ⚡ Интерактивные элементы

### Hover состояния

```jsx
// Для карточек - усиление тени
<Card className="shadow-sm hover:shadow-md transition-shadow cursor-pointer" />

// Для элементов меню
<button className="hover:bg-sidebar-accent hover:text-sidebar-accent-foreground transition-colors" />
```

### Transitions (Переходы)

```jsx
// Плавные переходы для интерактивных элементов
<div className="transition-all duration-200">
  {/* content */}
</div>

// Для появления страниц
<div className="animate-in fade-in duration-500">
  {/* Page content */}
</div>
```

### Active состояния

```jsx
// Активная ссылка в меню
<Link
  className={
    isActive
      ? "bg-sidebar-primary text-sidebar-primary-foreground shadow-sm"
      : "text-muted-foreground hover:bg-sidebar-accent"
  }
/>
```

### Анимированные элементы

```jsx
// Пульсирующий индикатор (текущая задача)
<div className="border-2 border-primary animate-pulse" />

// Смещение при появлении
<div className="animate-in slide-in-from-left duration-500" />
```

---

## 📚 Практические советы

### 1. Цветовое кодирование для статусов

Используйте цвета последовательно по смыслу:

```
🟢 Готово / Успех       → bg-emerald-500
🔵 В процессе / Активно → bg-primary (индиго)
🟠 Предупреждение       → bg-amber-500
🔴 Ошибка / Удаления   → bg-rose-500
```

### 2. Контрастность текста

Всегда проверяйте контрастность:
- Основной текст должен быть не менее 4.5:1 к фону
- Использование `text-muted-foreground` для вспомогательного текста допускает 3:1

```jsx
// ❌ Плохо - малый контраст
<p className="text-gray-400">Текст</p>

// ✅ Хорошо - используйте готовые переменные
<p className="text-muted-foreground">Текст</p>
```

### 3. Максимальная ширина контента

Избегайте очень длинных линий текста (>80 символов):

```jsx
<div className="max-w-4xl mx-auto">
  {/* Основной контент, читаемый */}
</div>
```

### 4. Пустые состояния

Всегда добавляйте дружелюбные сообщения для пустых состояний:

```jsx
{items.length === 0 ? (
  <div className="text-center py-12">
    <h3 className="text-lg font-semibold mb-2">Нет данных</h3>
    <p className="text-muted-foreground">Добавьте первый элемент</p>
  </div>
) : (
  // Список элементов
)}
```

### 5. Загрузочные состояния

Используйте Spinner или Skeleton для состояний загрузки:

```jsx
import { Spinner } from '@/components/ui/spinner';

{isLoading ? <Spinner /> : <Content />}
```

### 6. Мобильная адаптивность

Всегда использовуйте классы `hidden`, `flex`, `block` с breakpoints:

```jsx
{/* Скрыто на мобильных, видно на десктопе */}
<div className="hidden md:block">Десктоп</div>

{/* Мобильное меню */}
<Sheet>
  <SheetTrigger asChild>
    <Button variant="ghost" size="icon" className="md:hidden">
      <Menu />
    </Button>
  </SheetTrigger>
</Sheet>
```

### 7. Темный режим

CSS переменные поддерживают темный режим автоматически:

```jsx
// В .dark классе цвета переопределяются
<html className="dark">
```

Компоненты Shadcn/UI автоматически адаптируются.

### 8. Icons (Иконки)

Используйте `lucide-react` с правильными размерами:

```jsx
import { Plus, Check, X, AlertCircle } from 'lucide-react';

<Plus className="w-4 h-4" />           // Маленькие, в кнопках
<Check className="w-5 h-5" />          // Среднии, в иконках
<AlertCircle className="w-6 h-6" />    // Большие, в героях
```

---

## 🛠️ Как создать похожий дизайн

### Шаг 1: Установите базовые цвета

```css
/* В index.css */
:root {
  --primary: 230 70% 55%;         /* Ваш основной цвет */
  --background: 210 40% 98%;      /* Светлый фон */
  --foreground: 222 47% 11%;      /* Тёмный текст */
  --muted-foreground: 215 16% 47%; /* Серый текст */
  --border: 214 32% 91%;          /* Серая граница */
}
```

### Шаг 2: Выберите шрифты

```html
<!-- В index.html -->
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&family=Inter:ital,opsz,wght@0,14..32,100..900" rel="stylesheet">
```

Замените на свои шрифты, если нужно:
- **Заголовки**: Выберите смелый, современный шрифт (Poppins, DM Sans, Outfit)
- **Текст**: Выберите чистый, читаемый шрифт (Inter, Roboto, Open Sans)

### Шаг 3: Создайте Layout компонент

```jsx
// client/src/components/layout/Layout.tsx
export function Layout({ children }) {
  return (
    <div className="min-h-screen flex">
      <Sidebar />
      <div className="flex-1 flex flex-col">
        <Header />
        <main>{children}</main>
      </div>
    </div>
  );
}
```

### Шаг 4: Используйте Shadcn/UI компоненты

Они уже встроены в проект. Стилизуйте их через CSS:

```jsx
<Card className="border-border/50 shadow-sm hover:shadow-md transition-shadow">
  {/* Card content */}
</Card>
```

### Шаг 5: Добавьте микро-взаимодействия

```jsx
<div className="transition-all duration-200 hover:shadow-lg cursor-pointer">
  {/* Элемент с гладкими эффектами */}
</div>
```

---

## 📖 Файлы для изучения

| Файл | Описание |
|------|---------|
| `client/index.html` | Meta tags и импорт шрифтов |
| `client/src/index.css` | Переменные цветов и базовые стили |
| `client/src/components/layout/Layout.tsx` | Главный компонент макета |
| `client/src/pages/dashboard.tsx` | Пример использования компонентов |
| `vite.config.ts` | Конфигурация для вспомогательных стилей |

---

## 🎓 Ключевые выводы

✅ **Делайте**:
- Используйте CSS переменные для цветов
- Тестируйте контрастность текста
- Добавляйте transitions для интерактивности
- Проверяйте на мобильных размерах
- Соблюдайте spacing-систему

❌ **Не делайте**:
- Не смешивайте цвета без системы
- Не игнорируйте WCAG стандарты
- Не используйте жёсткие тени (`shadow-lg`)
- Не забывайте адаптивность
- Не перегружайте анимациями

---

**Успехов в создании красивых интерфейсов! 🚀**
