# TeamSync

TeamSync - это современное веб-приложение для управления проектами и задачами с интуитивным интерфейсом Kanban-доски.

## Особенности

- 📋 Управление проектами и задачами
- 🎯 Kanban-доски с_drag & drop функциональностью
- 👥 Назначение исполнителей и наблюдателей
- 💬 Комментарии и история изменений
- 📎 Прикрепление файлов
- 🏷️ Теги и приоритеты задач
- 📊 Статистика и отчеты
- 🌙 Темная/светлая тема
- 📱 Адаптивный дизайн

## Технологии

### Frontend
- React 18
- TypeScript
- Vite
- Tailwind CSS
- Radix UI Components
- TanStack Query
- Wouter (роутинг)

### Backend
- Node.js
- Express.js
- PostgreSQL
- Drizzle ORM
- Vercel Serverless Functions

## Быстрый старт

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone <ваш-репозиторий>
cd teamsync
```

2. Установите зависимости:
```bash
cd replit
npm install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими данными
```

4. Запустите базу данных (если используете локальную):
```bash
docker-compose up -d
```

5. Инициализируйте базу данных:
```bash
npm run db:init
```

6. Запустите приложение в режиме разработки:
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Деплой на Vercel

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Настройте переменные окружения в Vercel Dashboard:
   - `DATABASE_URL` - URL вашей PostgreSQL базы данных
   - `SESSION_SECRET` - секретный ключ для сессий
4. Деплой произойдет автоматически после каждого пуши в основную ветку

## Структура проекта

```
teamsync/
├── replit/
│   ├── api/                 # Vercel serverless functions
│   ├── client/             # React клиентское приложение
│   │   └── src/
│   │       ├── components/ # Переиспользуемые компоненты
│   │       ├── pages/      # Страницы приложения
│   │       └── lib/        # Вспомогательные библиотеки
│   ├── server/             # Серверные утилиты
│   ├── shared/             # Общие типы и схемы
│   ├── script/             # Скрипты сборки и миграции
│   └── uploads/            # Загруженные файлы
├── .gitignore
└── README.md
```

## Разработка

### Доступные скрипты

- `npm run dev` - запуск в режиме разработки
- `npm run build` - сборка для продакшена
- `npm run db:init` - инициализация базы данных
- `npm run db:create-tables` - создание таблиц
- `npm run db:test` - тестирование подключения к БД

### Работа с базой данных

Проект использует Drizzle ORM для работы с PostgreSQL. Схема базы данных находится в `shared/schema.ts`.

Для миграций используйте скрипты в папке `script/`.

## Деплой

### Подготовка к деплою

Перед деплоем убедитесь, что проект подготовлен:

```bash
npm run deploy:prepare
```

Этот скрипт проверит все необходимые файлы и создаст недостающие конфигурации.

### Переменные окружения для Vercel

В Vercel Dashboard необходимо настроить следующие переменные:

- `DATABASE_URL` - URL вашей PostgreSQL базы данных
- `SESSION_SECRET` - секретный ключ для сессий (минимум 32 символа)
- `NODE_ENV` - установить в `production`

### Автоматический деплой

1. Создайте репозиторий на GitHub
2. Закоммитьте все изменения
3. Подключите репозиторий к Vercel
4. Деплой запустится автоматически после каждого пуши

## Лицензия

MIT

## Контакты

Для вопросов и предложений обращайтесь к разработчику.