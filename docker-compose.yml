services:
  # Backend API Server
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: m4portal-server
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: ${DATABASE_URL:-postgresql://user:password@host:port/database}
      SESSION_SECRET: ${SESSION_SECRET:-dev_secret_key_change_in_production}
      REDIS_URL: ${REDIS_URL:-}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:8090,http://localhost:3000}
      YANDEX_CLIENT_ID: ${YANDEX_CLIENT_ID:-}
      YANDEX_CLIENT_SECRET: ${YANDEX_CLIENT_SECRET:-}
      APP_URL: ${APP_URL:-http://localhost:8090}
    ports:
      - "3245:3000"
    networks:
      - m4portal-network
    depends_on:
      - client

  # Frontend Client with Nginx
  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    container_name: m4portal-client
    restart: unless-stopped
    ports:
      - "8090:80"
    networks:
      - m4portal-network

networks:
  m4portal-network:
    driver: bridge
